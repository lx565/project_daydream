# 合盘 v2 — 多关系类型 + 钩子变现 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把现有合盘升级为支持5种关系类型、含趣味内容（感情模式/前世缘）、并用"免费钩子→¥29解锁"做第一个变现测试。

**Architecture:** 四维得分走确定性算法（扩展现有 `lib/couple.ts`）；preview 端点免费生成钩子内容，full 端点（改造现有 `/api/reading/couple`）付费后生成深度解读；客户端用现有 `usePaywall` + `PaywallLock` 门控，复用 ¥29 支付码。

**Tech Stack:** Next.js 15 (App Router) · TypeScript · SSE 流式 · DeepSeek (AI_PROVIDER) · Vercel KV (unlock) · iztro (命盘数据)

## Global Constraints

- **无单元测试框架** — 本仓库无 vitest/jest，只有 Playwright（截图用）。每个任务的"验证"= `npx tsc --noEmit` 类型检查 + 本地 dev server 手动验证。不要新建测试框架（违反仓库既有模式 + Niki "不要加没要求的东西"）。
- **复用 ¥29 支付码** — 不新建二维码资产、不改价格。`PaywallLock` 仅参数化 `included` 列表。
- **客户端门控** — 沿用现有模式：reading 路由**不**加服务端 401；`gated = paywall.enabled && !paywall.unlocked`。
- **合盘 chartId** = `${sessionId}_couple`（已是现有 SSE 缓存键），unlock key 复用 `unlock:{chartId}`。
- **四维得分确定性** — 由 `lib/couple.ts` 算法生成，preview/full/分享卡片三处必须一致。
- **ANTI_CLICHE** — 任何新增 AI 提示词避免模板腔（参考 `lib/seoContent.ts` 的 `ANTI_CLICHE`）。
- **部署后** bump `CACHE_VERSION`（`lib/sseWriter.ts`，现 v23）+ `CACHE_PREFIX`（`lib/useSSEStream.ts`，现 `ziwei_rd_v15_`），因合盘 prompt 结构改变。
- 简体中文输出；命理规则：七杀/天相/天府 不参与十干四化；紫微只 化权(壬)/化科(乙)。

---

## File Structure

| 文件 | 动作 | 职责 |
|---|---|---|
| `lib/coupleTypes.ts` | 新建 | 5种关系类型的配置（维度名/宫位/是否前世缘/RAG主题/钩子模板） |
| `lib/couple.ts` | 修改 | 新增 `calcCoupleScoreV2(...,type)` 返回4维命名得分 + 缘分类型标签 |
| `components/PaywallLock.tsx` | 修改 | `INCLUDED` 改为可选 prop `included`（默认不变） |
| `app/api/reading/couple/preview/route.ts` | 新建 | 免费：算分 + AI生成感情模式2行×2 + 钩子句 |
| `app/api/reading/couple/route.ts` | 修改 | full：加 relationshipType + 飞化互入/三方四正/大运 + 分享卡片块 |
| `components/FortuneForm.tsx` | 修改 | couple mode 加关系类型选择器 + URL param |
| `app/result/page.tsx` | 修改 | 读 `rel` param 传给 CoupleResultView |
| `components/CoupleResultView.tsx` | 修改 | preview/full 两段 + PaywallLock 门控 + 分享卡片 |

---

## Task 1: 关系类型配置 `lib/coupleTypes.ts`

**Files:**
- Create: `lib/coupleTypes.ts`

**Interfaces:**
- Produces:
  - `type RelationshipType = "lover" | "spouse" | "friend" | "sibling" | "parentchild"`
  - `interface RelationshipConfig { key, label, dimensions: [string,string,string,string], palaces: string[], hasPastLife: boolean, ragTopic: string, shareLabel: string, focusHint: string }`
  - `const RELATIONSHIP_TYPES: Record<RelationshipType, RelationshipConfig>`
  - `function getRelationshipConfig(t: string): RelationshipConfig`（未知值回退到 `lover`）

- [ ] **Step 1: 写文件**

```typescript
// lib/coupleTypes.ts
// 合盘关系类型配置。四维得分名称、重点宫位、是否生成前世缘故事、RAG 主题词
// 都按关系类型区分，供 preview/full 路由与得分算法共用。

export type RelationshipType =
  | "lover"      // 情侣/恋人
  | "spouse"     // 夫妻
  | "friend"     // 朋友/闺蜜
  | "sibling"    // 兄弟姐妹
  | "parentchild"; // 亲子

export interface RelationshipConfig {
  key: RelationshipType;
  label: string;                       // 中文名（UI 卡片 + 分享卡片）
  emoji: string;                       // UI 卡片图标
  dimensions: [string, string, string, string]; // 四维得分名称（顺序固定）
  palaces: string[];                   // 重点分析宫位（紫微）
  hasPastLife: boolean;                // 是否生成"前世缘"故事
  ragTopic: string;                    // RAG 检索主题词
  shareLabel: string;                  // 分享卡片标题副词，如"缘分类型"
  focusHint: string;                   // 给 AI 的关系侧重提示
}

export const RELATIONSHIP_TYPES: Record<RelationshipType, RelationshipConfig> = {
  lover: {
    key: "lover",
    label: "情侣 · 恋人",
    emoji: "💞",
    dimensions: ["吸引力", "默契度", "稳定度", "成长潜力"],
    palaces: ["夫妻", "命", "子女", "福德"],
    hasPastLife: true,
    ragTopic: "夫妻",
    shareLabel: "缘分类型",
    focusHint: "聚焦两人的吸引力来源、相处默契、感情稳定度与共同成长空间；可谈正缘时机。",
  },
  spouse: {
    key: "spouse",
    label: "夫妻",
    emoji: "💍",
    dimensions: ["稳定度", "子嗣缘", "家庭运", "白头到老"],
    palaces: ["夫妻", "子女", "田宅", "命"],
    hasPastLife: true,
    ragTopic: "夫妻",
    shareLabel: "姻缘类型",
    focusHint: "聚焦婚姻稳定度、子嗣缘分、共同家运与长久相守之道。",
  },
  friend: {
    key: "friend",
    label: "朋友 · 闺蜜",
    emoji: "🤝",
    dimensions: ["默契度", "互补性", "长久性", "互相成就"],
    palaces: ["交友", "命", "福德"],
    hasPastLife: false,
    ragTopic: "交友",
    focusHint: "聚焦两人性情默契、能力互补、友谊能否长久、是否互相成就。",
    shareLabel: "友缘类型",
  },
  sibling: {
    key: "sibling",
    label: "兄弟姐妹",
    emoji: "👫",
    dimensions: ["手足情深", "互帮互助", "缘分深浅", "相处模式"],
    palaces: ["兄弟", "命", "六亲"],
    hasPastLife: false,
    ragTopic: "兄弟",
    focusHint: "聚焦手足情分深浅、能否互相扶持、相处中的张力与化解。",
    shareLabel: "手足缘",
  },
  parentchild: {
    key: "parentchild",
    label: "亲子",
    emoji: "👨‍👧",
    dimensions: ["亲缘深度", "教育契合", "前世羁绊", "共同成长"],
    palaces: ["父母", "子女", "命"],
    hasPastLife: true,
    ragTopic: "父母",
    focusHint: "聚焦亲子缘分深浅、教养方式契合度、彼此牵绊与共同成长；语气温暖。",
    shareLabel: "亲缘类型",
  },
};

export function getRelationshipConfig(t: string | undefined): RelationshipConfig {
  if (t && t in RELATIONSHIP_TYPES) return RELATIONSHIP_TYPES[t as RelationshipType];
  return RELATIONSHIP_TYPES.lover;
}
```

- [ ] **Step 2: 类型检查**

Run: `cd ~/Desktop/Projects/fortune-app && npx tsc --noEmit`
Expected: 无新增报错（该文件无依赖问题）

- [ ] **Step 3: 提交**

```bash
git add lib/coupleTypes.ts
git commit -m "feat(couple): add relationship type config"
```

---

## Task 2: 四维得分算法 `lib/couple.ts`

**Files:**
- Modify: `lib/couple.ts`（在现有 `calcCoupleScore` 之后追加）

**Interfaces:**
- Consumes: 现有 `dayMasterAffinity`/`elementComplementarity`/`weddingPalaceScore`（同文件，需把这三个 helper 保持可见——它们已在模块作用域内，无需导出）；Task 1 的 `RelationshipType`/`getRelationshipConfig`。
- Produces:
  - `interface CoupleScoreV2 { type: RelationshipType; total: number; label: string; color: string; dims: { name: string; score: number }[]; weddingStarsA: string[]; weddingStarsB: string[]; }`
  - `function calcCoupleScoreV2(baziA, ziweiA, baziB, ziweiB, type): CoupleScoreV2`
  - `function yuanfenLabel(total: number, type: RelationshipType): string`（缘分类型标签，按 total 分档）

- [ ] **Step 1: 追加代码到 `lib/couple.ts` 末尾**

```typescript
// ── v2: relationship-aware 4-dimension scoring ──────────────────────────────
import { getRelationshipConfig, type RelationshipType } from "./coupleTypes";

export interface CoupleScoreV2 {
  type: RelationshipType;
  total: number;                 // 0–100
  label: string;                 // 缘分类型标签
  color: string;
  dims: { name: string; score: number }[]; // 4 项，名称来自 coupleTypes
  weddingStarsA: string[];
  weddingStarsB: string[];
}

// 缘分类型标签——按总分分档，文案随关系类型微调
function yuanfenLabel(total: number, type: RelationshipType): string {
  const lover = type === "lover" || type === "spouse";
  if (total >= 85) return lover ? "命中注定型" : "天生一对型";
  if (total >= 75) return lover ? "深度契合型" : "默契知己型";
  if (total >= 62) return lover ? "互补成长型" : "相辅相成型";
  return "需要经营型";
}

// 把三个确定性子分（日主/五行/夫妻宫均值）按关系类型映射到 4 个命名维度。
// 每个维度用一个稳定的加权组合 + 小幅偏移，保证：同一对命盘结果恒定、
// 4 个维度有区分度、落在 42–98 区间。
export function calcCoupleScoreV2(
  baziA: BaziResult, ziweiA: ZiweiResult,
  baziB: BaziResult, ziweiB: ZiweiResult,
  type: RelationshipType,
): CoupleScoreV2 {
  const cfg = getRelationshipConfig(type);
  const dm = dayMasterAffinity(baziA.dayMasterElement, baziB.dayMasterElement);
  const el = elementComplementarity(baziA.elements, baziB.elements);
  const aw = weddingPalaceScore(ziweiA);
  const bw = weddingPalaceScore(ziweiB);
  const wed = (aw.score + bw.score) / 2;

  const clamp = (n: number) => Math.min(98, Math.max(42, Math.round(n)));

  // 4 个维度各用不同权重，避免四格雷同；权重之和=1
  const raw = [
    dm.score * 0.5 + wed * 0.3 + el.score * 0.2,   // 维度1 偏"关系底色"
    dm.score * 0.3 + el.score * 0.3 + wed * 0.4,   // 维度2 偏"互动契合"
    wed * 0.5 + dm.score * 0.3 + el.score * 0.2,   // 维度3 偏"稳定/深度"
    el.score * 0.45 + dm.score * 0.35 + wed * 0.2, // 维度4 偏"成长/潜力"
  ];
  const dims = cfg.dimensions.map((name, i) => ({ name, score: clamp(raw[i]) }));
  const total = Math.round(dims.reduce((s, d) => s + d.score, 0) / 4);

  let color = "#6b7280";
  if (total >= 85) color = "#8B1A1A";
  else if (total >= 75) color = "#d97706";
  else if (total >= 62) color = "#0891b2";

  return {
    type, total, label: yuanfenLabel(total, type), color, dims,
    weddingStarsA: aw.stars, weddingStarsB: bw.stars,
  };
}
```

> 注：`import` 语句放文件顶部更规范，但 TS 允许文件中部 import 仅在顶层；若 lint 报错，把 `import { getRelationshipConfig, type RelationshipType } from "./coupleTypes";` 移到文件第 1-3 行的现有 import 区。

- [ ] **Step 2: 把 import 移到文件顶部**（与现有 import 并列，避免 lint 报错）

在 `lib/couple.ts` 顶部现有两行 import 后加：
```typescript
import { getRelationshipConfig, type RelationshipType } from "./coupleTypes";
```
并删除 Step 1 代码块中部的那行 import。

- [ ] **Step 3: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无新增报错

- [ ] **Step 4: 快速行为验证（临时脚本，验证后删除）**

```bash
cat > /tmp/t.mjs <<'EOF'
// 仅验证 4 维有区分度、落在区间内、同输入结果恒定
import { RELATIONSHIP_TYPES } from "./lib/coupleTypes.ts";
console.log(Object.keys(RELATIONSHIP_TYPES));
EOF
npx tsx /tmp/t.mjs && rm /tmp/t.mjs
```
Expected: 打印 5 个关系 key

- [ ] **Step 5: 提交**

```bash
git add lib/couple.ts
git commit -m "feat(couple): add relationship-aware v2 scoring"
```

---

## Task 3: 参数化 PaywallLock 的 included 列表

**Files:**
- Modify: `components/PaywallLock.tsx`

**Interfaces:**
- Consumes: 无新依赖
- Produces: `PaywallLock` 新增可选 prop `included?: string[]`（默认 = 现有常量）。价格/二维码不变。

- [ ] **Step 1: 改 Props + 常量**

把 `components/PaywallLock.tsx` 顶部：
```typescript
interface Props {
  chartId: string;
  sectionLabel?: string;
}
```
改为：
```typescript
interface Props {
  chartId: string;
  sectionLabel?: string;
  included?: string[];
}
```
把常量 `const INCLUDED = [...]` 改名为 `const DEFAULT_INCLUDED = [` （内容不变）。

- [ ] **Step 2: 函数签名 + 使用处**

把 `export default function PaywallLock({ chartId, sectionLabel }: Props) {` 改为：
```typescript
export default function PaywallLock({ chartId, sectionLabel, included = DEFAULT_INCLUDED }: Props) {
```
把 JSX 里 `{INCLUDED.map((item) =>` 改为 `{included.map((item) =>`。

- [ ] **Step 3: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无报错（现有调用未传 `included`，走默认值，行为不变）

- [ ] **Step 4: 提交**

```bash
git add components/PaywallLock.tsx
git commit -m "feat(paywall): parametrize included list, keep ¥29 default"
```

---

## Task 4: 免费预览端点 `/api/reading/couple/preview`

**Files:**
- Create: `app/api/reading/couple/preview/route.ts`

**Interfaces:**
- Consumes: `calcCoupleScoreV2`（Task 2）、`getRelationshipConfig`（Task 1）、现有 `makeSSEResponse`/`streamWithRefs`（`lib/sseWriter.ts`）、`getKnowledge`（`lib/rag.ts`）、`checkRateLimit`（`lib/rateLimit.ts`）。
- Produces: POST 端点，body = `{ baziA, ziweiA, baziB, ziweiB, nameA?, nameB?, genderA, genderB, relationshipType }`，SSE 输出一段 Markdown：缘分类型 + 四维分 + 甲乙感情模式各2行 + 钩子句。无解锁检查。

> 参考现有 `app/api/reading/couple/route.ts` 的结构（SYSTEM + userMessage + makeSSEResponse）。

- [ ] **Step 1: 写路由**

```typescript
// app/api/reading/couple/preview/route.ts
// 免费预览：确定性算出四维分 + 缘分类型，AI 只写"感情模式（各2行）+ 钩子句"。
// 不做解锁检查。钩子句对有前世缘的关系类型做截断，吊起付费欲望。
import { MODERN_INSTRUCTION } from "@/lib/modernInstruction";
export const maxDuration = 30;

import { NextRequest } from "next/server";
import { checkRateLimit, rateLimitResponse } from "@/lib/rateLimit";
import { makeSSEResponse, streamWithRefs } from "@/lib/sseWriter";
import { calcCoupleScoreV2 } from "@/lib/couple";
import { getRelationshipConfig, type RelationshipType } from "@/lib/coupleTypes";
import type { BaziResult } from "@/lib/bazi";
import type { ZiweiResult } from "@/lib/ziwei";

const SYSTEM = `你是资深命理师。下面会给你两个人的命盘要点与一个"关系类型"。
请只输出以下三块，简短克制，不要展开长篇（这是免费预览，要留白吊起好奇）：

## 各自的感情模式
**[甲方称呼]**：用2行描述这个人在该关系里的相处模式与情感倾向（落到具体星曜/日主，不空泛）。
**[乙方称呼]**：同样2行。

## 缘分一瞥
用1-2句点出两人关系的最大亮点或张力（不下吉凶定论）。

最后单独一行，以"——"开头写一句"钩子"，戛然而止、引人想看下文，例如：
——你们之间，似乎有一段更早的渊源……

简体中文。不要出现"付费/解锁/完整版"等字样。` + MODERN_INSTRUCTION;

const BRANCH_ZODIAC: Record<string, string> = {
  子:"鼠",丑:"牛",寅:"虎",卯:"兔",辰:"龙",巳:"蛇",午:"马",未:"羊",申:"猴",酉:"鸡",戌:"狗",亥:"猪",
};

export async function POST(request: NextRequest) {
  if (!(await checkRateLimit(request, { limit: 3, keyPrefix: "couple-preview" })).allowed) return rateLimitResponse();

  let body: {
    baziA: BaziResult; ziweiA: ZiweiResult;
    baziB: BaziResult; ziweiB: ZiweiResult;
    nameA?: string; nameB?: string; genderA: string; genderB: string;
    relationshipType?: string;
  };
  try { body = await request.json(); }
  catch { return Response.json({ error: "invalid_request" }, { status: 400 }); }

  const { baziA, ziweiA, baziB, ziweiB, nameA, nameB, genderA, genderB, relationshipType } = body;
  const cfg = getRelationshipConfig(relationshipType);
  const score = calcCoupleScoreV2(baziA, ziweiA, baziB, ziweiB, cfg.key as RelationshipType);

  const labelA = nameA || (genderA === "male" ? "甲方（男）" : "甲方（女）");
  const labelB = nameB || (genderB === "male" ? "乙方（男）" : "乙方（女）");

  const userMessage = `
【关系类型】${cfg.label}（侧重：${cfg.focusHint}）

【确定性得分】缘分类型：${score.label}（${score.total}分）
${score.dims.map(d => `${d.name} ${d.score}`).join(" · ")}

【${labelA}】生肖${BRANCH_ZODIAC[baziA.year.branch] ?? ""}，日主${baziA.dayMaster}（${baziA.dayMasterElement}），命格：${baziA.summary}
夫妻宫主星：${score.weddingStarsA.join("、") || "（空宫）"}

【${labelB}】生肖${BRANCH_ZODIAC[baziB.year.branch] ?? ""}，日主${baziB.dayMaster}（${baziB.dayMasterElement}），命格：${baziB.summary}
夫妻宫主星：${score.weddingStarsB.join("、") || "（空宫）"}

请按系统要求输出"各自的感情模式 + 缘分一瞥 + 钩子句"。称呼用「${labelA}」「${labelB}」。`.trim();

  return makeSSEResponse((writer, encoder) =>
    streamWithRefs(writer, encoder, {
      maxTokens: 700,
      temperature: 0.7,
      system: SYSTEM,
      messages: [{ role: "user", content: userMessage }],
      refs: [],
    })
  );
}
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无报错

- [ ] **Step 3: 本地手动验证**

启动 dev（若未运行）：`npm run dev`。用浏览器走一次合盘（或下一任务完成后），确认 `/api/reading/couple/preview` 返回 SSE 文本。临时验证可在浏览器 Network 看流。
Expected: 流式返回含"各自的感情模式"+ 钩子行的 Markdown

- [ ] **Step 4: 提交**

```bash
git add app/api/reading/couple/preview/route.ts
git commit -m "feat(couple): add free preview endpoint"
```

---

## Task 5: 升级 full 端点 `app/api/reading/couple/route.ts`

**Files:**
- Modify: `app/api/reading/couple/route.ts`

**Interfaces:**
- Consumes: Task 1 `getRelationshipConfig`、Task 2 `calcCoupleScoreV2`。
- Produces: 同路径 POST，新增 body 字段 `relationshipType`；SYSTEM/userMessage 按关系类型定制；新增飞化互入、三方四正、大运时机段；输出末尾追加分享卡片文本块（以固定标记 `### 分享卡片` 开头，便于前端切出）。

- [ ] **Step 1: 读现有文件**，在 `POST` 内解析 body 处加 `relationshipType`，取 `cfg = getRelationshipConfig(relationshipType)` 和 `score = calcCoupleScoreV2(...)`。

在 body 类型里加：
```typescript
    relationshipType?: string;
```
解构处加 `relationshipType`，并在 `const branchA = ...` 之前插入：
```typescript
  const cfg = getRelationshipConfig(relationshipType);
  const score = calcCoupleScoreV2(baziA, ziweiA, baziB, ziweiB, cfg.key);
```
文件顶部 import 区加：
```typescript
import { getRelationshipConfig } from "@/lib/coupleTypes";
import { calcCoupleScoreV2 } from "@/lib/couple";
```

- [ ] **Step 2: 改 SYSTEM 提示词**——把现有 `const SYSTEM = ...` 替换为关系类型感知 + 新增段落版：

```typescript
const SYSTEM = `你是精通紫微斗数与八字子平的资深合盘命理师。本次合盘的关系类型会在用户信息中给出，请始终扣住该关系类型的侧重来解读（情侣谈感情、亲子谈教养、朋友谈默契，不要套同一模板）。

分析层次：
1. 独立看甲方在这段关系中的模式（不受乙方影响）
2. 独立看乙方
3. 两盘合观：契合、张力、相处之道
4. 时机：结合双方当前大运，点出关系的高峰/考验阶段

请严格按以下 Markdown 输出（标题照抄）：

## 四维详解
（逐条解释用户给出的四维得分各自因何而来，落到具体星曜/日主/五行；每维1-2句；**加粗**维度名）

## 甲方在这段关系中
（约160字：夫妻宫/相关宫主星与四化、感情星强弱、相处模式；**加粗**关键星曜）

## 乙方在这段关系中
（约160字，同框架）

## 飞化互入
（约140字：一方的生年四化/重要星曜落入对方命盘的哪个宫，说明彼此牵动的领域。注意命理规则：七杀/天相/天府不参与十干四化；紫微只化权/化科。）

## 合盘综析与三方四正
（约180字：双方相关宫位的三方四正牵引、日主生克、四柱合冲、五行互补结构；**加粗**关键论断）

## 缘分时机
（约120字：结合双方当前大运，点出关系的高峰期与需留心的阶段）

## 相处之道
（针对两人命盘，3-5条具体可操作建议；- 开头列表）
$PASTLIFE$

## 给你们的话
（温暖真诚的收尾寄语，约70字）

### 分享卡片
（严格按以下格式，方括号替换为实际内容，不加其他文字：）
✦ 命里合盘 ✦
[甲方称呼] × [乙方称呼]
$SHARELABEL$：[缘分类型标签]
[维度1名] [分] · [维度2名] [分] · [维度3名] [分] · [维度4名] [分]
"[一句12字内的缘分点睛，温暖且可截图分享]"
仅供传统文化学习参考 · mingli.study

简体中文。` + MODERN_INSTRUCTION;
```

并在构造 SYSTEM 后、或在 userMessage 前，用关系配置替换占位符：
```typescript
  const pastLifeSection = cfg.hasPastLife
    ? "\n## 前世缘分（命理故事）\n（约120字：以来因宫/夫妻宫星曜组合为依据，写一段富画面感的"前世今生"小叙事，开头注明这是命理意象、非史实；温暖动人，适合分享）"
    : "";
  const systemPrompt = SYSTEM
    .replace("$PASTLIFE$", pastLifeSection)
    .replace("$SHARELABEL$", cfg.shareLabel);
```
把 `streamWithRefs(... system: SYSTEM ...)` 改为 `system: systemPrompt`。

> 注意：现有代码把 `SYSTEM` 直接传给 `streamWithRefs`。改用 `systemPrompt` 变量。

- [ ] **Step 3: 在 userMessage 注入关系类型 + 四维分**

在现有 userMessage 模板的【合盘数据】块前加入：
```typescript
【关系类型】${cfg.label}　侧重：${cfg.focusHint}
【四维得分（确定性，请据此解释）】缘分类型：${score.label}（${score.total}分）
${score.dims.map(d => `${d.name} ${d.score}`).join(" · ")}
```
（拼进现有 `userMessage` 字符串即可。）

- [ ] **Step 4: RAG 主题按关系类型**——把现有 `getKnowledge({... topic: "夫妻" ...})` 的 `topic` 改为 `cfg.ragTopic`，`text` 末尾追加 `cfg.label`。

- [ ] **Step 5: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无报错

- [ ] **Step 6: 提交**

```bash
git add app/api/reading/couple/route.ts
git commit -m "feat(couple): relationship-aware deep reading + share card"
```

---

## Task 6: FortuneForm 加关系类型选择器

**Files:**
- Modify: `components/FortuneForm.tsx`

**Interfaces:**
- Consumes: `RELATIONSHIP_TYPES`（Task 1）
- Produces: couple 提交时 URL 带 `&rel=<type>`。

- [ ] **Step 1: import + state**

顶部 import 加：
```typescript
import { RELATIONSHIP_TYPES, type RelationshipType } from "@/lib/coupleTypes";
```
在 `// Couple` state 区（约 line 113）加：
```typescript
  const [relType, setRelType] = useState<RelationshipType>("lover");
```

- [ ] **Step 2: 提交时带 param**

在 `handleSubmit` 的 couple 分支（`params.set("method", "couple");` 之后）加：
```typescript
      params.set("rel", relType);
```

- [ ] **Step 3: 选择器 UI**——在 couple mode JSX 块（`{/* ── 合盘 mode ── */}` 区，约 line 294 的说明文字 `输入两人信息…` 之后）插入：

```tsx
          <div className="space-y-2">
            <label className={labelClass}>关系类型 <span className="text-vermillion">*</span></label>
            <div className="grid grid-cols-3 gap-2">
              {(Object.values(RELATIONSHIP_TYPES)).map((r) => (
                <button
                  key={r.key}
                  type="button"
                  onClick={() => setRelType(r.key)}
                  className={`flex flex-col items-center gap-1 rounded-lg border px-2 py-2.5 text-xs transition-all ${
                    relType === r.key
                      ? "border-vermillion bg-vermillion-l text-vermillion font-semibold"
                      : "border-border-warm bg-paper text-ink-3 hover:border-vermillion/40"
                  }`}
                >
                  <span className="text-lg leading-none">{r.emoji}</span>
                  <span>{r.label}</span>
                </button>
              ))}
            </div>
          </div>
```

- [ ] **Step 4: 类型检查 + 视觉验证**

Run: `npx tsc --noEmit`
然后 `npm run dev`，首页切到"合盘 · 缘分"tab，确认出现 5 个关系卡片、可选中高亮。
Expected: 选择器渲染正常，提交后 URL 含 `rel=`

- [ ] **Step 5: 提交**

```bash
git add components/FortuneForm.tsx
git commit -m "feat(couple): relationship type selector in form"
```

---

## Task 7: result 页透传 relationshipType

**Files:**
- Modify: `app/result/page.tsx`

**Interfaces:**
- Consumes: URL param `rel`
- Produces: 给 `CoupleResultView` 传 `relationshipType` prop。

- [ ] **Step 1: 读 param**

`interface SearchParams` 加 `rel?: string;`。在解构 `params` 处（约 line 56）加 `rel` 不必，直接在 couple 渲染分支用 `params.rel`。

- [ ] **Step 2: 传 prop**

在 couple 分支 `<CoupleResultView ... sessionId={sessionId} />` 加：
```tsx
          relationshipType={params.rel}
```

- [ ] **Step 3: 类型检查**（CoupleResultView 还没加该 prop，会报错——下一任务修）

Run: `npx tsc --noEmit`
Expected: 仅剩"CoupleResultView 不接受 relationshipType"一类错误，Task 8 解决。可与 Task 8 连续做后再一起验证。

- [ ] **Step 4: 提交**

```bash
git add app/result/page.tsx
git commit -m "feat(couple): pass relationshipType to result view"
```

---

## Task 8: CoupleResultView — preview/full + 付费门控 + 分享卡片

**Files:**
- Modify: `components/CoupleResultView.tsx`

**Interfaces:**
- Consumes: `usePaywall`、`PaywallLock`、Task 1 `getRelationshipConfig`、Task 2 `calcCoupleScoreV2`、两个端点 `/api/reading/couple/preview` 与 `/api/reading/couple`。
- Produces: 完整付费流页面。

- [ ] **Step 1: import + props**

顶部加：
```typescript
import { usePaywall } from "@/lib/usePaywall";
import PaywallLock from "./PaywallLock";
import { getRelationshipConfig } from "@/lib/coupleTypes";
import { calcCoupleScoreV2 } from "@/lib/couple";
```
`interface Props` 加 `relationshipType?: string;`，函数签名解构加 `relationshipType`。

- [ ] **Step 2: 算分 + 配置 + 门控状态**

把现有 `const score = calcCoupleScore(...)` 替换为：
```typescript
  const cfg = getRelationshipConfig(relationshipType);
  const score = calcCoupleScoreV2(baziA, ziweiA, baziB, ziweiB, cfg.key);
  const coupleChartId = `${sessionId}_couple`;
  const paywall = usePaywall(coupleChartId);
  const gated = paywall.enabled && !paywall.unlocked;
```

- [ ] **Step 3: 两条 SSE 流**

把现有单条 `const reading = useSSEStream("/api/reading/couple", `${sessionId}_couple`)` 改为两条：
```typescript
  const preview = useSSEStream("/api/reading/couple/preview", `${coupleChartId}_preview`);
  const full = useSSEStream("/api/reading/couple", coupleChartId);
```
把自动启动 effect 改为：preview 总是跑；full 仅在未被门控时跑。
```typescript
  useEffect(() => {
    if (preview.status === "idle") {
      preview.start({ baziA, ziweiA, baziB, ziweiB, nameA, nameB, genderA, genderB, relationshipType: cfg.key });
    }
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  useEffect(() => {
    if (!paywall.loading && !gated && full.status === "idle") {
      full.start({ baziA, ziweiA, baziB, ziweiB, nameA, nameB, genderA, genderB, relationshipType: cfg.key });
    }
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [paywall.loading, gated]);
```

- [ ] **Step 4: 四维得分卡片**——把现有 score card 的三个 `DimRow`（日主/五行/夫妻宫）替换为按 `score.dims` 渲染、缘分类型用 `score.label`：

```tsx
          <div className="flex-1 w-full space-y-3">
            {score.dims.map((d) => (
              <DimRow key={d.name} label={d.name} score={d.score} desc="" />
            ))}
          </div>
```
并把 `ScoreRing score={score.total} color={score.color}` 下方 `{score.label}` 保留（现 v2 也有 `label`）。`DimRow` 的 `desc` 现为空串——把 `DimRow` 的 `<p>{desc}</p>` 包一层 `{desc && (...)}` 避免空段。标题"合盘缘分指数"下副标题加 `{cfg.shareLabel}：{score.label}`。

- [ ] **Step 5: 解读区——preview 永远显示，full 门控**

把现有"合盘 AI 解读"块替换为：
```tsx
      {/* 免费预览 */}
      <div>
        <p className="text-xs text-ink-4 tracking-widest uppercase mb-2 px-1 flex items-center gap-2">
          <span className="w-px h-3 bg-vermillion inline-block" />
          <span className="text-vermillion">合盘 · 抢先看</span>
        </p>
        <div className="paper-card rounded-2xl border border-border-warm p-4 sm:p-5">
          {(preview.status === "streaming" || preview.status === "idle") && <CoupleLoadingSkeleton />}
          {preview.status === "done" && <CoupleReadingText text={preview.text} />}
          {preview.status === "error" && <p className="text-sm text-vermillion">{preview.errorMsg}</p>}
        </div>
      </div>

      {/* 完整解读 / 付费门控 */}
      <div>
        <p className="text-xs text-ink-4 tracking-widest uppercase mb-2 px-1 flex items-center gap-2">
          <span className="w-px h-3 bg-gold inline-block" />
          <span className="text-gold">合盘 · 完整解读</span>
        </p>
        {gated ? (
          <PaywallLock
            chartId={coupleChartId}
            sectionLabel="合盘完整解读"
            included={[
              "四维缘分 · 逐项详解",
              "双方在这段关系中的模式",
              "飞化互入 · 彼此牵动的领域",
              cfg.hasPastLife ? "前世缘分 · 命理故事" : "三方四正 · 深度合观",
              "缘分时机 · 大运高峰与考验",
              "相处之道 · 可操作建议",
            ]}
          />
        ) : (
          <div className="paper-card rounded-2xl border border-border-warm p-4 sm:p-5">
            {(full.status === "streaming" || full.status === "idle") && <CoupleLoadingSkeleton />}
            {full.status === "done" && <CoupleFullReading text={full.text} labelA={labelA} labelB={labelB} />}
            {full.status === "error" && (
              <div className="space-y-2">
                <p className="text-sm text-vermillion">{full.errorMsg}</p>
                <button onClick={() => full.start({ baziA, ziweiA, baziB, ziweiB, nameA, nameB, genderA, genderB, relationshipType: cfg.key })}
                  className="text-xs text-gold underline">重试</button>
              </div>
            )}
          </div>
        )}
      </div>
```

- [ ] **Step 6: 分享卡片切分组件**——在文件内加 `CoupleFullReading`，把 `### 分享卡片` 之后的内容切出来单独渲染成可复制卡片：

```tsx
function CoupleFullReading({ text, labelA, labelB }: { text: string; labelA: string; labelB: string }) {
  const marker = "### 分享卡片";
  const idx = text.indexOf(marker);
  const body = idx >= 0 ? text.slice(0, idx) : text;
  const card = idx >= 0 ? text.slice(idx + marker.length).trim() : "";
  return (
    <div className="space-y-4">
      <CoupleReadingText text={body} />
      {card && <ShareCard text={card} />}
    </div>
  );
}

function ShareCard({ text }: { text: string }) {
  const [copied, setCopied] = useState(false);
  async function copy() {
    try { await navigator.clipboard.writeText(text); setCopied(true); setTimeout(() => setCopied(false), 1500); } catch {}
  }
  return (
    <div className="rounded-xl border-2 border-gold/40 bg-gradient-to-b from-gold-l/30 to-paper p-4">
      <pre className="whitespace-pre-wrap font-sans text-sm text-ink leading-relaxed text-center">{text}</pre>
      <button onClick={copy}
        className="mt-3 w-full rounded-full bg-vermillion px-4 py-2 text-xs font-semibold text-white hover:opacity-90 transition-opacity">
        {copied ? "已复制 ✓" : "复制 · 分享到小红书"}
      </button>
    </div>
  );
}
```
（`useState` 已在文件顶部 import。）

- [ ] **Step 7: 移除旧的 `calcCoupleScore` import**（已换成 v2），并删除"重新生成"按钮相关旧 `reading.*` 引用，避免悬空变量。

- [ ] **Step 8: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无报错（含 Task 7 的 prop 现已满足）

- [ ] **Step 9: 全流程手动验证**

`npm run dev`，首页合盘 → 选每种关系类型各跑一次 → 确认：预览先出（含钩子）、四维卡片用对应维度名、付费墙在 `NEXT_PUBLIC_PAYWALL_ENABLED` 关闭时不挡（直接出完整解读 + 分享卡片）。
Expected: 5 种关系都正常；分享卡片可复制。

- [ ] **Step 10: 提交**

```bash
git add components/CoupleResultView.tsx
git commit -m "feat(couple): preview/full split, paywall gating, share card"
```

---

## Task 9: 部署前收尾

**Files:**
- Modify: `lib/sseWriter.ts`（CACHE_VERSION）、`lib/useSSEStream.ts`（CACHE_PREFIX）

- [ ] **Step 1: bump 两层缓存版本**

`lib/sseWriter.ts`：`const CACHE_VERSION = "v24";`（注释加日期+原因）
`lib/useSSEStream.ts`：`const CACHE_PREFIX = "ziwei_rd_v16_";`

- [ ] **Step 2: 构建验证**

Run: `npm run build`
Expected: 构建成功，无类型错误

- [ ] **Step 3: 截图核对（可选，复用脚本）**

参考 `scripts/xhs-shots.mjs` 思路，但合盘页 AI 是流式、无头模式难等到——以本地浏览器人工核对为准。

- [ ] **Step 4: 提交 + 部署**

```bash
git add lib/sseWriter.ts lib/useSSEStream.ts
git commit -m "chore(couple): bump cache versions for v2 reading"
npx vercel --prod --yes
```

- [ ] **Step 5: 部署后冒烟**

线上走一遍合盘，确认 5 种关系类型与分享卡片正常。`NEXT_PUBLIC_PAYWALL_ENABLED=true` 时确认 preview 出、full 被 PaywallLock 挡、扫码填单号后解锁 full。

---

## Self-Review 覆盖检查

- ✅ 5 种关系类型 → Task 1 配置 + Task 6 选择器
- ✅ 四维得分确定性、preview/full/卡片一致 → Task 2（单一 `calcCoupleScoreV2`）
- ✅ 感情模式 → preview（2行）+ full（详解，不同 altitude，不算冗余）
- ✅ 前世缘 → full 的 `$PASTLIFE$`（仅 lover/spouse/parentchild）
- ✅ 飞化互入/三方四正/大运时机 → Task 5 新增段
- ✅ 免费钩子→¥29解锁 → Task 8 门控复用 usePaywall/PaywallLock，¥29 不变
- ✅ 分享卡片可复制 → Task 8 ShareCard
- ✅ 客户端门控、无服务端 401 → 与现有模式一致
- ✅ 缓存双层 bump → Task 9
- ⚠️ Niki 手动 TODO：付费打开前在 Vercel 确认 `NEXT_PUBLIC_PAYWALL_ENABLED`；合盘解锁与个人解读共用 ¥29 收款码，对账时 Google Sheet 里 sessionId 带 `_couple` 后缀可区分来源。
